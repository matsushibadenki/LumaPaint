{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Basic local window capability; no filesystem or shell access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
