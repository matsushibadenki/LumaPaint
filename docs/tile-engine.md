# ラスタータイルと非破壊ドキュメントの移行契約

## 現在の実装範囲

`lumapaint-core::tiles` は256×256の疎なRGBA8タイルを持つ。キャンバス外の矩形、長さが異なる画素入力、古い状態に対するUndo/Redo差分を拒否する。透明画素のRGBは0へ正規化し、全透明タイルは割り当てない。書き込みは変更したタイルの前後だけを返し、差分全体を検査してからUndo/Redoへ適用する。右端・下端のタイルも内部バッファは256×256で、キャンバス外の画素は透明のままにする。

`TiledRasterDocument` は最大16枚の独立したラスターレイヤーを保持し、レイヤーIDを指定した矩形編集を一つの履歴操作にする。Undo/Redoは最後に編集したレイヤーの変更タイルのみを復元し、変更したレイヤーIDとタイル座標を返す。これは将来GPUキャッシュを部分更新するための無効化情報になる。無変更の書き込みは履歴・revisionを増やさず、新しい編集ではRedoを破棄する。

`TiledRasterState`は文書寸法、レイヤー順・設定、疎なRGBA8画素タイルと8bitマスクタイルを履歴なしで受け渡す。復元時は文書寸法・レイヤー数・ID・濃度・座標・固定ペイロード長を検査し、重複タイル、全透明／全表示なのに割り当てられたタイル、右端・下端のキャンバス外に値を持つタイルを文書へ採用する前に拒否する。圧縮・データ位置・チェックサムは次期コンテナ側で検査し、この状態型へは検査済みのペイロードだけを渡す。

`tile_container`は現行v1と独立した試験用バイナリ形式として、この状態をマニフェストと連続タイルペイロードへ変換する。形式識別子、版、マニフェスト／全体サイズ、マニフェストと各タイルのCRC32、オフセット、固定長を検査する。参照間の隙間、重なり、範囲外参照、未参照の末尾データ、切断と改変は状態復元前に拒否する。現段階では非圧縮RGBA8／8bitだけを扱い、通常の保存・復旧ファイルにはまだ使用しない。

Tauriのプロジェクトファイル層には、タイルコンテナ用の上限付き読込と原子的書込APIを追加した。共通読込は形式識別子でv1 JSONとタイルコンテナを分岐する。macOSの「開く」は検証済みタイル形式をv1へ渡さず、画面セッション未接続のエラーを返して開いている文書を維持する。自動復旧ワーカーはタイル状態を専用ジョブとして受け取り、同じ原子的書込で保存できる。復旧候補も形式判定付きで読み戻し、従来APIへタイル形式を渡さない。タイル文書をOpenDocumentへ保持し、画面から開く・保存・復旧する処理は次の統合作業で追加する。

指定座標のタイルは、レイヤー順に通常のアルファ合成を行ったRGBA8の乗算済み画素として取得できる。未割り当て座標は画素バッファを作らず、キャンバス端の余白は透明を保つ。GPU側の部分転送には、編集・Undo/Redoが返す無効化座標だけを再合成する。

`prepare_uploads`は無効化座標を検証・重複排除し、各座標の合成済み乗算RGBA8画素、作品上の転送位置、端で切り抜く幅・高さ、1024バイトの行ピッチを返す。非表示・マスク・Undoで画素が消えた場合もゼロ埋め画素を返し、GPU上の古い内容を消せる。新規GPUテクスチャには`prepare_full_uploads`で初期内容を作る。レンダラーの`TileTexture`は全転送データを検証してからwgpuの該当領域へ書き込み、macOS Metal上で端タイルと透明化を読戻して確認した。
レンダラーには明示的な`install_tiled_preview`／`update_tiled_preview`／`clear_tiled_preview`を設けた。有効時はv1ストロークの代わりにタイルテクスチャを作品座標で描き、SVGレイヤーと選択枠を従来の順序で重ねる。タイル専用シェーダーはRGBA8のsRGB直線色を復元し、線形色へ変換してからアルファ乗算・合成する。Metalのオフスクリーン描画で端画素の位置と透明化を確認した。1:1表示の硬い線・柔らかい色付き線・交差の比較では、色成分の平均差がそれぞれ0.21／0.32／1.08、最大差が7／2／8だった。

同じMetal比較を50%・200%表示とRetina相当でも実施した。硬い線の平均色成分差はそれぞれ21.34／10.10／5.99で、最大差は148／185／75だった。1:1では別の中央配置線でも平均0.28・最大7に収まる。これはタイル側のアンチエイリアスを作品画素幅で確定してから拡大縮小する一方、v1ブラシは画面画素に合わせて輪郭幅を計算するため。柔らかい線の平均差は50%／200%／Retinaで5.67／3.27／0.28だった。倍率を変えても同等に見せるには、表示倍率に応じたタイル解像度か描画時の輪郭再計算が必要。macOSホストからはまだ有効化しておらず、アクティブな未確定ストロークもタイルプレビューには含めない。複雑なレイヤー合成もホスト切替前に確認する。

物理画面画素数が作品画素の0.75倍未満のときだけ、タイルのサンプラーを最近傍に切り替える。Metalの50%表示では硬い線の平均差が21.34から7.71、最大差が148から110へ下がった。柔らかい線の平均差は5.67から5.87へわずかに増える。Retina相当では最近傍の方が硬い線の差を悪化させたため、線形補間を維持する。この切替は暫定的な縮小表示の改善であり、他倍率の輪郭差は残る。

`project_committed_paint_layer_at_scale`は確定済みv1ストロークのブラシ点と保存済み選択範囲を1倍または2倍の一時タイルへ写す。元の作品寸法、入力座標、保存形式は変更しない。レンダラーは論理作品寸法とタイル寸法の比率を検査してから2倍テクスチャを作品全体へ貼る。macOS Metalでの200%表示では、2倍タイルにより硬い線の平均色成分差が10.10から4.11、柔らかい線が3.27から0.29へ下がった。Retina相当でも物理的には作品1画素が画面約1画素の条件では2倍化が逆効果だったため、プレビューはRetina属性そのものではなく実際の画面画素密度を使う。

macOSホストは画面上で作品1画素が約1または2物理画素になる場合に限り、確定済みペイントの一時タイルを専用スレッドで再構成する。投影対象は最大1677万画素に制限する。完了時には文書ID、revision、キャンバストークン、倍率を再検査し、古い結果を描画しない。ブラシ入力中や未対応倍率ではv1ストロークを表示し、タイル再構成・GPU転送に失敗してもv1へ戻る。タイルは保存形式へ書き込まない。

表示用タイルとGPUテクスチャはブラシ入力中も保持する。確定時にrevisionがちょうど1進み、前回からストロークが1本増え、作品寸法・描画レイヤー設定・表示倍率が同じで、ロックや透明ピクセル保護がない場合だけ追記する。選択範囲を含む新ストロークのタイル計算は専用ワーカーへ移し、変更タイルだけをメインスレッドでGPUへ転送する。1倍・2倍の両方で追記後の全タイル転送データが全量再構成と一致することを検査した。同一文書・キャンバス・寸法・倍率でrevisionが1進むその他の編集では、ワーカーが新旧の合成画素を比較して変化したタイルのみ転送する。Undoで消えた画素には透明な転送データを作る。文書切替・倍率変更・比較不能な編集・追記失敗時は古いGPUプレビューを破棄して非同期の全量再構成へ戻す。

別IDのmacOS検証アプリで二本のブラシとUndo/Redo、100%／125%表示、文書切替を操作し、古いタイルの残留や描画消失がないことを画面で確認した。計測付き実行で最初の全量投影はワーカー27.61ms・GPU転送15.97ms。二本目は変更タイル2枚の差分投影でワーカー13.02ms・GPU転送8.54msだった。ストローク確定時の描画フレームは約16.42ms、転送後の描画フレームは約7.41msだった。数値はApple M4での一回の診断であり、継続的な性能保証ではない。

タイルの全量再構成・差分追記・新旧比較では、合成済み画素と転送矩形の作成もタイルワーカー内で完了させる。メインスレッドは文書ID・revision・キャンバス世代・倍率を再検査してからGPUへ書き込む。従来の`install_tiled_preview_at_scale`／`update_tiled_preview`も残し、ワーカーを使わない呼び出しの互換性を維持する。上記の時間はこの変更前の計測値であり、変更後の実機再計測は未実施。

新旧比較の実装後、別IDのmacOS検証アプリで125%表示の二本のブラシをUndo/Redoし、消えた線の残留がなく、Redoで戻ることを画面で確認した。転送時間の再計測は後続作業とする。

1ストロークのUndo/Redoでは、前後のレイヤー表示・不透明度・マスク・ロックが同じ場合に限り、該当ストロークのサンプリング済みブラシ点を使って候補タイルを作る。候補はラスタ化と同じ2画素の余裕を持ち、選択範囲による切り抜きは過小評価しないよう候補から差し引かない。新旧合成の比較は候補内だけ行い、候補算出に失敗した場合や他の編集では全タイル比較に戻る。1倍・2倍で全比較と転送結果が一致する回帰テストを追加した。全ストロークの再投影とGPU転送自体は従来どおりで、時間の再計測は未実施。

候補タイル限定後も、別IDのmacOS検証アプリで125%表示の二本の線をUndo/Redoし、消去と復元を画面で確認した。

同一文書・キャンバス・寸法・倍率でrevisionが1進み、確定ストローク数とレイヤー表示・不透明度・マスク設定が不変なら、ペイントの合成画素は変わらない。ベクター編集やロック変更などでは保持中のタイルテクスチャをそのまま表示し、ワーカーへの再投影とGPU転送を省く。ロックと透明ピクセル保護は将来の入力を制限するだけなので比較条件から除外するが、保持中のタイル文書のロック状態には反映し、次のストロークにも正しい設定を渡す。表示・不透明度・マスク変更やストローク増減では従来の差分経路を使う。

別IDのmacOS検証アプリで125%表示のペイント線を描き、レイヤーロックを有効にしても線が消えず、解除後に追加の線を描けることを画面で確認した。

ストローク数が変わらず合成設定が変わる1 revision編集では、保持済みタイル文書の表示・不透明度・均一マスク・ロック設定をワーカーで更新する。割当済みタイルの変更前後を合成比較し、変化した画素だけをGPUへ転送する。元ストロークの再投影は行わない。1倍・2倍で、表示のオン／オフ、不透明度、均一マスクの変更が全量再投影と同じ転送画素になることを検査した。空間マスクなど新しい編集種別が画面につながる段階では、変更座標を別途渡す必要がある。

転送バッチのタイル座標・端タイル寸法・行幅・画素長は、合成を終えたワーカーで一括検証してからメインスレッドへ渡す。検証済み型だけがタイルごとの再検証を省略でき、既存の生の転送APIは従来どおりGPU書き込み前に全件検証する。文書寸法とプレビュー倍率の検査はメインスレッドでも維持する。GPU書き込み自体の時間は今後再計測する。

別IDのmacOS検証アプリで125%表示の二本の線を描き、Undoで二本目が消えること、レイヤーの非表示・再表示で残る線が正しく消えて戻ることを画面で確認した。

別IDのmacOS検証アプリで125%表示の線を非表示・再表示し、レイヤー不透明度42%へ変更して線が薄くなることを画面で確認した。

タイル文書内では各レイヤーの表示と不透明度、後ろから前への重ね順を変更できる。表示・不透明度の変更はそのレイヤーの割り当て済みタイル、重ね順変更は移動範囲内の全レイヤーのタイルを無効化する。画素編集と同じUndo/Redo履歴に積み、各操作の逆操作でも対応する座標を返す。未変更の設定や無効な不透明度・移動先は文書revisionを変えない。

各レイヤーのロックはブラシと画素置換を拒否する。透明ピクセル保護中は画素置換を拒否し、ブラシが既存の不透明画素のRGBだけを変更してアルファを維持する。両設定は画素転送を要しない履歴操作としてUndo/Redoに積み、変更タイルは空で返す。

既存レイヤー設定と同じ有効化・反転・濃度を持つマスクを合成時に適用する。さらに各レイヤーに場所ごとに編集できる疎な8bitマスクを保持する。未編集画素は255（全表示）で、全画素が255のマスクタイルは割り当てない。マスク値に濃度を掛け、反転時はその結果を反転する。元のRGBAタイル画素は変更せず、マスク編集と設定変更をUndo/Redoに積み、影響するタイルのみを無効化する。ブラシ点は通常の描画と同じ曲線・弧長・硬さ・濃度と選択範囲を使い、指定した0〜255のマスク値へ一ストローク単位で合成する。画面のマスクブラシ操作は未接続。

サンプリング済みのブラシ点を`RasterDab`（中心・筆圧反映後の半径・硬さ・光学濃度の重み）として一括でタイルへ描ける。一本のストローク内では濃度を先に足し、既存画素へ一度だけ合成するため、自己交差を別の不透明レイヤーとして重ねない。変更タイルの差分は一つのUndo/Redo操作になる。無効な点が含まれる場合は描画前に全件を拒否し、重み0やキャンバス外だけなら履歴を増やさない。

既存GPUブラシと同じ曲線補間・弧長サンプリングから`RasterDab`を生成するアダプターを設けた。保存済みストロークの選択範囲はタイル画素の中心で判定し、加算・減算・反転をブラシの濃度蓄積前に適用する。選択範囲とブラシ点は描画前に検証し、無効な入力で文書履歴を変更しない。

`project_committed_paint_layer`はv1文書の確定済みペイントストロークをタイルへ再生し、レイヤーの表示・不透明度・ロック・均一マスク設定を写す。非表示レイヤーのストロークも保持し、再生中のタイル履歴は都度破棄する。移行結果は元文書へ書き戻さず、アクティブな入力ストローク、SVG／ベクターレイヤー、保存revisionは対象外とする。

これは共通コア内の独立した文書モデルであり、現在のブラシ描画・画面のレイヤー・保存ファイルはまだストローク／SVGのv1モデルを使う。したがって、タイル操作だけでアプリの表示や保存内容が変わることはない。

## 次の接続

1. 疎なマスクとロック・透明ピクセル保護を既存の画面レイヤー操作へ接続し、マスクブラシの入力をタイル文書へ送る。レイヤーの並べ替えではタイルを複製しない。
2. 差分転送時のメインスレッド時間を減らし、編集内容から影響タイルを直接特定して新旧比較の計算量も減らす。200%以上に残る硬い輪郭の差は、ブラシ形状を別に保持して画面画素で輪郭を描く案も検討する。
3. v1ファイルは従来どおり読み込む。タイルを永続化する段階で新しい形式バージョンを導入し、マニフェストにドキュメント寸法・レイヤー順・色空間・処理ノード・タイル参照を記録する。各タイルの座標・形式・サイズ・チェックサムを検証してから文書へ採用し、未知のノードや破損した参照は黙って描画しない。
4. 処理グラフは元画像／ベクター／文字、マスク、フィルター、合成を別ノードにする。ノード入力の変更時は影響するタイルのキャッシュだけを無効化する。AI生成の入力・モデル・結果参照も後続のノード種別として扱い、結果画像だけで元の設定を失わないようにする。正本・グラフ・保存形式の詳細は[非破壊ドキュメント実装契約](non-destructive-document.md)に定める。

現段階のRGBA8は既存のブラシとの接続用であり、最終的な作業色空間やRGBA16F、ICC/CMYKの実装を確定するものではない。形式移行時は色空間と画素形式をタイルごとに明記する。
