[package]
name = "lumapaint-renderer"
version.workspace = true
edition.workspace = true

[dependencies]
lumapaint-core = { path = "../lumapaint-core" }
wgpu = "27"
bytemuck = { version = "1", features = ["derive"] }
resvg = "0.45"

[dev-dependencies]
pollster = "0.4"
